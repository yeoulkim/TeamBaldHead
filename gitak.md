#include <iostream>

using namespace std;

//합계함수
int getsum(int arr[], size);
int sum;
for (int i = 0; i < size; i++)
	sum += arr[i];

return sum;


//평균



int main() {
	const int size = 5;
	int numbers[size];

	cout << "숫자 5개를 입력하세요";
	cin >> numbers[i];

	int total = getsum(numbers, size)
		int average = getaverage(numbers, size)

		cout << "합계: " << total << endl;
	cout << "평균: " << total << endl;

	return 0;




}
